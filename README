CURRENT TASKS:
----------------------------
+ think over about design;
+ write a prototype of Makefile;
+ write a main-function's prototype;
+ fill a TOOLSET field;
+ plane a git's commits;

+ write a prototype of scheduler module;
	+ read about branch predictor;
	+ read about functional pointers;
+ write a database module;
	+ read about getting a parent_of();
+ write a sort module;
	+ bubbleSort();
	+ shellSort();
	+ quickSort();
	+ compile it;
+ write an input/output interface;
	+ read about output's format;
	+ read about va_list from <stdarg.h>;
	- read about VAL(varues array's length);
	- need to partially rewrite;
+ write a file input/output inteface;
+ make an error handling;
	+ error's type:
		* allocation memory/file-work, crucial errors(TYPE1);
		reaction(call to uninitScheduler(int err_code)):
			1. save a current database;
			2. close all file descriptors;
			3. uninitalization all static objects;
			4. print(strerror(errno));
			5. call exit(errno);
		* allocatiom memory/file-work/input-errors, non-crucial errors;
		reaction(TYPE2):
			1. print(strerror(errno));
			2. errno=0;
			3. return control to caller;
		* user's error, a like input's error(TYPE3);
		reaction:
			1. set input's error;
			2. print(input's error);
			3. try again;

		*input_error: 	invalid number,
				key_is_notvalid,
				id_alreay_exists;
+ write an universal rule;
- add a C89 standard;
- debug, just debug;
- append a scheduler module (aside sort());
	+ init/uninit functions;
	+ new_entry();
	+ edit_data();
	+ find_entry();
	+ delete_data();
	+ save_to_file();
	+ load_from_file();
	+ show_all();
	+ quit();
	+ errors handling;
	- compile it;
- write a test environment;
	- generator.h;
	- timer.h;
	- testio.h;
+ write a Makefile;
	+ read about a recipes;

- testing and debugging;
	- define a maximal number of entries;
	- think over about test's order;
	- append sort() to the scheduler module;

- done;
----------------------------

TOOLSET:
----------------------------
gcc (GCC) 10.2.0;
GNU Make 4.3;
VIM - Vi IMproved 8.2 
	(2019 Dec 12, compiled Jan 14 2021 18:17:04);
GNU gdb (GDB) 10.1;
git version 2.30.0;
----------------------------

DEBUG PLAN
------------------------------------------------------------------------------
------------------------------------------------------------------------------
+ creating a base;
	+ create base contains a 8 entries and saving it;
+ saving/reading a file;
	+ saving manually;
	+ autosaving;
	+ exit without autosaving;
	+ exit with autosaving;
	+ loading without saving a current base;
	+ loading with saving ...;
	+ loadind, add and saving;
	+ loading a wrong data;
- edit, remove, find a data;
	+ edit each attribute;
	+ remove 3 entries from begin, middle and end;
	+ find data;
		+ find by ID;
		+ find by string;
		
- (de)allocation a memory;
	- create bases contains a greater 4096 and 8192 bytes correspondly;
- display a many entries;
------------------------------------------------------------------------------

ACTUAL BUGS
------------------------------------------------------------------------------
------------------------------------------------------------------------------
------------------------------------------------------------------------------

PATCH-LOG
------------------------------------------------------------------------------
------------------------------------------------------------------------------
v0.31(current, commit):
	- fixed createDB();
	- fixed tabs;
	- fixed input order;
	- append dialog at saving;
	- debuged create, save, load;
v0.32(not commit):
	- fixed a binding downloaded base to new base;
	- treated a broken removing an entries;
------------------------------------------------------------------------------



VIM
----------------------------
ctrl+w:
	v	-	verically split screen;
	l	-	left to right;
	h	-	right to left;
